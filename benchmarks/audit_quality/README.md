# 合成审计效果验收

最新状态：用户已取消此前跨任务总调用次数限制，下方旧额度仅为历史记录。每次运行仍保留显式调用、时长和格式保护。三类样例已有核心supported/refuted/inconclusive结果，正例走完独立上下文复核；仍有评级、依赖子问题及定位质量问题，不声明完整质量通过。结果目录及核对说明见根目录agent-implementation-plan.md。

单次请求默认等待120秒，输出上限8192 token，分别用`--timeout-seconds`和`--max-output-tokens`设置。`--resume data/audit-checks/<运行ID> --resume-task <子任务ID>`从指定合成任务检查点续跑；不指定子任务时恢复原始任务。源码、仓库、版本和绑定必须仍然一致。父产物不覆盖，新产物保存在`continuations/<新任务ID>`。

正式Web链路可用`python -m benchmarks.audit_quality.run_web_check --case case-a --allow-model-calls`检查。它只访问本机8000端口：上传合成ZIP、调用生产索引流水线（包括Embedding）、混合搜索、创建审计。无需读取密钥，使用容器中的正式配置；已有审计运行时拒绝启动。所有数据使用新UUID范围，不替换旧仓库。测试仓库、源码快照和向量保留，便于在页面查看，不自动删除正式索引中的数据。

运行后用`--collect data/web-audit-checks/<运行ID>`只读检查任务状态；结束时保存报告。它不会自动重试或重建索引。收集器与压缩包有离线保护测试，但完整质量仍需核对真实报告。

新增完整链路脚本：`python -m benchmarks.audit_quality.run_agent_check --case case-a --max-calls 9`。
须先配置独立审计模型环境变量并启动本机OpenSearch；只有明确获准复用搜索配置时才加`--use-search-model`。
脚本不会读取`.env`，应由调用环境安全加载，不打印凭据。它会真实发起模型调用，不能作为普通单元测试运行。
使用独立测试索引、SQLite源码快照和正常AuditService；结束删除本次测试索引，保留`data/audit-checks/<运行ID>`中的报告和数据库。
它验证Agent与取证链路，不调用向量服务、不验证生产索引流程。预算不足必须记为未完成，不能改写成质量通过。

这些样例验证模型推理质量，不用于证明真实项目已完整审计。不得把脚本预设结论或格式测试当作模型效果。

三个独立任务使用同一审计目标：订单读取的跨用户访问控制。`cases.py` 包含完整合成代码、明确部署前提和独立期望答案；只能将 `model_input()` 返回的资料发送给模型，不能发送整个样例对象或文件源码（其中含答案）。

| 样例 | 预期 | 验收关注点 |
| --- | --- | --- |
| case-a | 静态支持缺少所有权控制 | 用户可控 ID → 查询 → 返回私有地址，定位返回前缺失的控制 |
| case-b | 反证否定该越权假设 | 发现返回前的所有者校验，不因危险模式或读取顺序误报 |
| case-c | 证据不足 | 主动追查策略实现；缺失实现不能被解释为无控制或已安全 |

执行要求：

1. 使用各自独立的仓库/版本、快照、索引及任务上下文，不共享其他样例的结论。
2. 模型必须使用正常取证工具，不能直接拿预期结论生成报告。检索命中不计为读取证据。
3. 每次运行明确设置调用与时长预算；网络失败也计入预算，不自动重试。真实调用须有费用及凭据使用授权。
4. 保留完整任务报告、调用计数和停止原因。失败、预算耗尽、没有相关调查结论均归为未完成，不能把“没发现”计为负样例通过。
5. 人工核对来源、路径、关键行、攻击者权限、数据流、反证和独立验证。case-a仅有宽泛假设不算通过；case-b必须确实读到并解释防护；case-c须点明未提供的策略实现。
6. 三例结果分开记录，既报正确结果，也报误报、漏报、证据不足及流程失败。单次三样例不能推断总体准确率。

当前状态（2026-09-04）：用户本次仅授权总上限10次，并允许复用现有重排序 API；`probe.py` 首次请求在60.09秒后网络失败/超时，随后停止，总请求1次，没有可评价模型结论。剩余样例未运行；不得报告任何准确率。探针直接提供合成代码，不是完整Agent流程。未使用真实仓库、未执行样例代码、未显示用户密钥内容。后续运行需重新核对授权额度。
