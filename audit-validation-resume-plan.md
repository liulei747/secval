# 独立复核恢复：实施与验收

## 已核对的现状

- 候选详情改变拒绝复用已有直接单测：输入指纹一致但 candidate_detail 标题变化时，重新调用模型而不是沿用旧结论。至此验收标准中“范围/清单/批次改变拒绝”“详情改变拒绝复用”“并行顺序”“部分失败重做”均有测试证据。共 422 项通过。
- 部署验收：队列检查为空后重建 secval-api，health healthy；容器内确认失败占位移除与重做逻辑已上线。本条覆盖上一条“未部署”。未重建历史索引，未调用模型。至此复核恢复链路覆盖：完成即落盘、指纹复用、失败重做、验证阶段原地恢复、全部失败边界测试。
- 部分失败复核恢复已通过端到端验收：父任务中第二个复核网络失败（error 记录落盘），续跑子任务复用第一个完成结果（reused=True），失败项自动移除占位并重新独立复核成功；最终两项齐全、顺序稳定。配套修复：验证阶段入口遇到失败占位记录时移除后重新复核，而不是跳过或继承失败。共 421 项测试通过；未部署，未调用真实模型。
- 部署后容器内回归：secval-api 容器内运行 3 项复核身份/证据/复用专项测试全部通过，确认镜像内代码包含指纹、reused 跳过与 as_completed 逐项落盘逻辑；容器内临时测试文件已清理（临时安装的 pytest 保留在镜像外层，不影响下次重建）。
- 部署验收：全量 418 项测试通过，两次队列检查为空后重建 secval-api（未重启其他服务）；health healthy，四个数据服务 available。上线内容：验证阶段原地恢复（reused 预置、跳过重复模型调用）、导出报告读取子任务持久化复核、完成即落盘顺序保持。调试脚本已清理；历史索引未重建，未调用模型。
- 验证阶段原地恢复已通过端到端合成验收：父任务提交两项复核后，续跑子任务直接预置 reused 记录并跳过重复模型调用（review 调用次数保持 2），报告导出来自子任务持久化的 independent_reviews。曾发现两处问题并修复：导出报告此前读取父任务残留值导致 KeyError；预置记录未标 reused 导致重复调用。报告仍按调查顺序稳定输出。
- 部署验收：全量 417 项测试通过后，两次队列检查为空重建 secval-api；health 返回 healthy 且四个数据服务 available。本轮上线内容：并行复核逐项落盘、复核输入/证据指纹、匹配复用与无效缓存回退、view_id 临时字段剔除。未重建历史索引，未调用模型；复用仍需调查检查点路径，验证阶段原地恢复待做。
- 复核上下文不再携带临时 scope.view_id（PIT 重开会变化），避免相同固定快照续跑时指纹必然失配；其余范围字段保留。测试确认 view_id 变化不改变指纹、index_run_id 变化仍失效、调用方上下文不被修改。
- 无效历史结果回退已补：指纹匹配但结果合同无效时跳过缓存，重新独立复核；非字典旧记录也跳过。测试先复现无效 outcome 导致异常，再验证重新调用模型，不将损坏记录当完成结果。
- 续跑已将父任务 independent_reviews 带入 previous_independent_reviews，串行与并行 review_packet 均接入输入及证据摘要校验，匹配时跳过模型并标 reused，不修改父记录。专项测试覆盖相同输入跳过调用、上下文改变重新调用。仍从调查检查点恢复，尚未支持直接回到验证阶段；补证未随检查点恢复时保守重新复核。仍需完整 Web 续跑验收。
- 复核结果已记录初始输入指纹及全部读取证据（包括补证）的原文/定位摘要。review_evidence_matches 拒绝缺失、内容变化、定位变化和旧版缺摘要的记录；两项专项测试通过。此校验尚未接入续跑复用，不能视为恢复功能完成。
- 完成顺序专项验收已通过：test_finished_second_review_is_saved_before_first_finishes 使用真实 SQLite 存储更新后发出事件，第一个脚本复核只有观察到第二个已落盘才允许结束；最终顺序仍为 investigation-1、investigation-2。旧的提交顺序等待实现会被此测试阻断。此次不调用远程模型。
- 已开始实现：audit_runner 使用 as_completed 接收并行复核，主线程每收到一项即保存并按调查顺序排序；取消时停止接收。审计模块 266 项测试通过。仍需专门验证后提交任务先落盘的时序，尚未实现输入身份与续跑复用。
- `services/agent_team.py` 与 `audit_service.py` 支持调查子任务恢复；不能据此宣称独立复核已恢复。
- `services/audit_runner.py` 在提交报告后执行 `validations = []`，逐候选重新安排独立复核。
- `services/audit_checkpoint.py` 只接受 baseline/investigation 阶段，STATE_FIELDS 不含独立复核结果。
- `restore_checkpoint` 已检查仓库、源码快照、索引批次、授权路径和源码清单，可以作为恢复前置条件。

## 实施顺序

1. 定义复核输入身份：固定范围、候选调查记录、边界、详情版本、用户前提、相关证据摘要。按稳定 JSON 计算摘要；不能仅按 investigation_id 复用。
2. 每个复核任务独立保存状态、输入身份、结果和新增证据。明确区分完成结论与网络失败、预算停止；错误结果不能冒充已完成复核。
3. 续跑创建子审计时保留父任务历史，将可核对的复核记录带入恢复上下文；输入一致且证据完整时复用，否则重新复核。
4. 验证阶段保存恢复检查点，绕过已经完成的主调查；候选详情有变化时使关联复核失效。
5. 并行复核完成一项就持久化一项，避免按提交顺序等待时丢失后完成任务的已知成果。取消后不接收迟到结果。

## 验收标准

- 两个候选中第一个完成、第二个超时：续跑只请求第二个，父报告与父记录保持不变。
- 候选编号相同但详情、边界、前提或证据改变：拒绝复用。
- 范围、清单或索引批次改变：继续由恢复前置检查拒绝。
- 模型结束前进程退出：不能假定未保存请求成功。
- 并行完成顺序与提交顺序不同：每项结果仍独立落盘，报告排序稳定。
- 用小型合成 demo 验证 Web 创建、取消、续跑、导出；记录实际模型调用量及复用量。

本文件是基于当前代码的待实施方案，不代表该能力已经完成。解析器剩余边界继续保留在主计划，先推进此项长任务恢复能力。
