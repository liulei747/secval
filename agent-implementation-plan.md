# 自建审计 Agent 实施计划

## 多Agent协作更新（优先于下方串行阶段记录）

2026-09-05：对照DeepSeek Harness的continuable宿主实现，新增子Agent阶段成果提交。候选、反证、未知项和审阅声明先校验并持久化，再向主调查精简回注；不再依赖最终大JSON一次成功。独立候选复核也已改为受Agent上限控制的并行请求，使用独立模型与局部证据缓冲，主线程再顺序合并；双候选并发屏障测试可以发现串行退化。原生`tools/tool_calls`因当前供应商方言尚未确认而未启用。

同日主线继续：审计只读工具已接入`hybrid_search`。它先固定OpenSearch PIT和源码绑定批次，再做BM25、向量召回与RRF；不调用外部重排序，不返回源码正文，实时候选只有在PIT中仍存在且批次、仓库、快照、路径一致才返回。随后完成Neo4j声明关系、Joern调用位置和静态数据流工具；模型不能执行原生图查询。当前完整回归248项通过，合成仓库真实检查得到1个调用位置、4条数据流和3条相关声明。Joern数据流覆盖层已显式保存，重建容器后结果数不变。

已按用户要求改造为Web默认协作执行：主Agent与独立基线/架构子任务并行，支持分派专项调查、结果先保存后注入、共享预算和子任务检查点续跑。
实现说明及小demo入口见根目录`agent-team-implementation.md`。保留旧串行任务兼容，不改变历史报告或已有用户索引。
真实小demo仓库为`secval-web-check-eb88a71402e840f6940e1dc4489fbcfb`，任务`2a7c604c025f4a6d9f3b26fc9558a17a`；只含两个合成Java文件，6个块/6个向量，没有删除旧块。
初始3个请求在33、48、64毫秒发出，已经证明真实模型请求重叠。架构完成，基线发生连续JSON错误；仍需核对最终报告，不能把并行可运行当作审计质量通过。

日期：2026-09-04。参考根目录交接文件及本机 Codex Security 0.1.22 的流程说明。
交接参考 0.1.25；本机参考版本不同，不能宣称逐项一致或能力等效。

## 最新执行状态（优先于下方历史记录）

- 用户已取消此前的跨任务总调用次数限制，继续推进；每个任务仍有调用、时长和连续格式错误保护，不盲目重试。
- 当前本机完整回归为191项通过，1个既有Starlette依赖弃用警告；真实OpenSearch与SQLite快照取证检查再次通过。旧文档的236/258项不能代替本机结果。
- 最新API已重建，包含输出预算、中文上下文优化、结果含义澄清、部分报告续跑、取消保护及2小时取证租约；健康检查、审计页面和续跑路由已复查。没有重建用户索引。
- 修复SQLite连接退出事务后未关闭的问题，避免Windows数据库锁；真实OpenSearch与SQLite快照取证检查通过。
- 三阶段共享只读工具定义，增加剩余预算提示；格式纠错由累计三次停止改为连续三次停止，总调用限制继续保留。
- 真实合成检查已走到调查结论，但候选详情曾被4096 token截断。现增加可配置8192 token输出预算；JSON中文改为原文发送，旧检查点可无损转换。
- 三类真实合成样例已取得报告，核心目标分别为supported/refuted/inconclusive。正例有匹配详情指纹的独立上下文支持发现；不能据此计算准确率或宣称完整质量通过，具体问题见下方记录。
- 中文JSON规范化后，同一正例续跑输入由约39700 token降到14700（约63%）；这不保证生成速度。保存动作回执只返回编号和状态，完整输入与结果仍留在历史记录。
- 正式独立审计模型由用户自行配置（本轮已明确）；合成验收按此前授权临时复用已有模型配置，不复制到正式审计环境。
- 第4阶段混合搜索、Neo4j和Joern已实现并完成合成服务验收；剩余重点是更完整的框架语义、多语言样例和跨进程任务队列。
- Web索引已有SQLite持久化后台任务、状态查询和显式续跑，并使用操作系统文件锁阻止同主机多个API进程同时替换索引。执行器仍不负责跨主机排队，也不会自动续跑中断任务，因此不是分布式队列。
- 索引任务已记录完整阶段时间线与失败阶段，并支持SQLite跨进程取消信号。取消只在新索引提交前生效；绑定或清理开始后拒绝取消。真实小Demo确认取消批次不可见、旧批次仍然可见。
- 索引工作进程现在原子认领任务，并在长步骤中持续写入心跳和租约；终态释放租约，恢复扫描不受任务列表分页影响。当前不自动抢占过期租约，避免在不清楚外部写入进度时重复执行。
- Web审计使用独立的操作系统文件锁：第二个API进程不会把第一个进程的运行中任务误标为中断，也不能同时启动另一项审计；取消任务后仍等待已发送模型请求退出再释放锁。多Agent仍在单个任务内部并行，这不等于跨主机调度。
- 审计任务运行信息已拆到独立SQLite表，支持原子认领、心跳、租约、尝试次数和不覆盖调查JSON的跨进程取消。模型请求退出后才最终释放租约；暂不自动抢占过期审计。
- 索引与审计提供统一租约状态和显式`recover-stale`：仅在租约过期且进程锁空闲时收口旧任务，绝不自动重跑；用户核对外部状态后再通过`resume`创建子任务。
- Python已接入Tree-sitter、结构化切块、固定快照取证和正式Web索引；纯Python小型Demo的搜索、Neo4j和Joern调用图已实测。混合仓库现按语言建Joern子项目并合并查询，Java/Python调用同时可见；跨语言调用边仍未建立。
- 新增固定规则的`find_entry_points`，用于定位Spring、JAX-RS、FastAPI/Flask和Django Web入口；它只提供线索，需继续读取源码核实路由和鉴权。
- 反例发现outcome含义歧义：模型支持“控制有效”，后端却按漏洞候选处理。共享明确的漏洞假设状态说明后，真实续跑修正为refuted，原始错误及修订历史均保留。
- 新增报告提交/部分报告/已登记检查项收口的区分；部分报告可显式续跑为子任务，保留父报告且不沿用旧独立复核。
- 取证视图租约由10分钟改为2小时，正常结束主动释放；旧租约短于允许的任务时长。取证服务故障单独分类，不自动回退实时索引。历史失败没有保存具体取证异常，不能确定其必然是租约过期。
- 补运行层取消后提前返回及回归测试；数据库层原已有冻结cancelled任务的保护，不能声称旧存储实现会覆盖取消状态。

## 合成验收结果与局限

| 样例 | 最新核心结果 | 核对情况 |
| --- | --- | --- |
| case-a 无归属检查 | supported，1条静态支持发现 | 已走候选详情与独立上下文复核；关键方法确实忽略身份参数、直接返回查询对象 |
| case-b 有归属检查 | refuted，0条发现 | 续跑明确解释返回前ownerUserId与服务端身份比较；修正旧supported误用 |
| case-c 缺失策略实现 | inconclusive，0条发现 | 读取接口、追查实现并保留缺口；未将未知实现提升为漏洞 |

产物：

- case-a：`data/audit-checks/25fe8ecc8350418d8b521e923af10864/continuations/aa2de9ed9fed41649297007faaa9c0fa/report.json`
- case-b：`data/audit-checks/1a8ff3a7c47c41fa9bce2f00e7337e8c/continuations/0da7d8a885a846ad91871a8fff61d331/report.json`
- case-c：`data/audit-checks/f5b8095698154b58a79bda2e32aff75d/report.json`

不能忽略的问题：case-a有数字ID可枚举的无证推断和整文件根因定位；case-b部分依赖子问题虽然声明实现缺失，仍给refuted，结论过强；case-c为未证实候选填low等级，不等于证据已证明低风险。三例均有未收口项；负例和未知例没有独立漏洞复核（没有supported候选），不冒充经过独立语义验证。报告的quality_passed保持未自动判定，历史失败不删除。

当前阶段边界：离线回归、统一只读工具、三类核心合成行为、Web任务生命周期、混合搜索、Neo4j声明关系和Joern调用/数据流已有实现与小样例证据。完整质量收口、真实项目Web验收、更完整的框架语义、多语言实测与分布式队列仍未完成。正式模型由用户配置，未取得真实仓库外发授权，不自行跨过这一步。

## 正式Web接口验收（合成代码）

用户配置已被容器加载，没有复制或打印凭据。通过正式HTTP接口上传2个合成Java文件、索引12个代码块及12个向量、混合搜索返回3项，未删除旧代码块。合成仓库`secval-web-check-bc373d7aa25c4a9c81e24b9473ab69a0`保留用于页面查看；不是用户真实仓库。

首个审计任务`2e6cad9e474d405891f80655489ca2c3`完成2次读取后，基线结果JSON无效，已保留父报告。基线示例改为严格JSON并补解析测试，子任务`9bdc7107b6a64d65959a13c527f02ba2`通过正式resume接口越过基线进入主调查，随后第11次请求等待响应头120.22秒超时；没有重新上传或重建索引，未完成正式审计报告。

新增modelRequests记录请求耗时、token数、正文长度与JSON错误行列，不保存思考或响应正文。它只反映本任务已保存统计；取消冻结或进程中断可能缺少尾部统计，不意味着未调用或未计费。产物保存在`data/web-audit-checks/bc373d7aa25c4a9c81e24b9473ab69a0`。

慢请求例：最终50字符的工具动作分别耗时85.37秒和107.76秒，供应端报告思考字符数27094与32034；不能把耗时全归因于上下文大小，也不能区分排队和生成各占多少。关闭思考的纯文本探针返回HTTP400，未改正式设置。已提供可选SECVAL_AUDIT_THINKING配置，默认留空不发送扩展。

用户已明确批准将正式单请求超时由120调整为300秒；本机.env已修改，API已重建并核对容器配置为300秒、8192输出上限、thinking为空。模型及凭据未改动。从上述失败检查点创建续跑子任务`24bf9765373d431682ea76256fbfb8a3`，没有重新上传或索引；创建成功不等于审计完成。

该子任务已失败：第1次主调查请求在300.25秒超时，持久化错误为“审计API请求超时（阶段：建立连接或等待响应头）”。输入18101字符，没有收到可解析响应。API及OpenSearch容器健康，Qdrant运行中；不能据此证明模型供应端正常，也无法区分连接、上游排队与非流式生成耗时。已收集该子任务报告，未自动重试或继续延长超时。

## 当前状态

自建运行时已有：基线调查、主调查、反证、独立上下文验证、证据约束报告、取消及子任务续跑。
取证工具已有：list_chunks、search_text、find_symbol、read_chunk、list_files、read_file、search_source。
文件正文来自绑定仓库、版本和索引批次的 SQLite 源码快照；不能以当前磁盘代替历史证据。
取证工具还包括：hybrid_search、find_code_relations、find_code_calls、find_data_paths。它们都绑定同一索引批次和源码快照，不允许模型提交任意Cypher或CPGQL。
当前完整回归248项通过，1个既有Starlette依赖弃用警告。

## 分阶段实施与验收

1. 验证基础：补模型故障分类与脱敏离线测试；确认本机实际测试基线。
   不自动重试，不提高预算，不把超时推断成模型能力不足。
2. 工具入口：整理统一能力定义和参数校验，区分线索、源码证据、关系结果和路径结果。
   验收：三个调查阶段工具定义一致；未接入能力明确不可用；范围外和版本不符时拒绝。
3. 完整链路：合成项目绑定真实索引和源码快照，通过正常AuditService进行正例、反例和缺失依赖例验证。
   离线模拟仅验证编排；真实模型调用必须另行确认凭据和调用预算。
   无报告不能算反例通过；必须核对工具阅读、反证和独立复核。
4. 关系与路径：分别接入Neo4j关系查询和Joern静态路径查询。
   每项结果绑定版本、分析批次、源码位置、分析器信息和限制；路径存在不等于可利用。
5. 长任务：分阶段预算、进度、取消和恢复，补齐API与页面验收后再部署。

## 边界

不执行被审计代码，不调用Codex内部工具作为部署依赖，不默认外发真实源码。
不重建用户现有索引，不把旧合成探针剩余额度当作新的调用授权。
源码查看与索引搜索并存；搜索负责定位，读取负责核实，关系和路径负责补充分析。
当前仅本机单用户原型，不能公开部署。

## 本轮执行记录

- 已完成本机状态核对：原有79项测试通过，而非交接中的258项。
- 已补模型网络故障分类和响应阶段标记；HTTP状态错误保留状态码，不回显原始异常。
- 已补10项离线测试，覆盖脱敏、不自动重试、正文读取中断及成功请求契约。
- 完整回归89项通过，存在一个既有Starlette测试依赖弃用警告；git diff --check通过。
- 新增tests/audit目录已从忽略规则中放行；其余未跟踪测试不批量纳入。
- 未发送模型请求，未部署容器，未修改用户索引。原始60秒故障原因尚未经真实请求确认。
- 下一步为第2阶段：统一工具能力定义与三个调查阶段的参数/能力声明，再补离线编排回归。
- 第1阶段只恢复了模型适配器测试；交接中其他缺失的审计回归仍需逐项恢复。

## 模型连通性与完整链路准备

- 当前主机使用已有搜索模型配置完成1次纯文本请求，1.77秒返回预期JSON；未发送源码。
- 独立审计配置仍未设置；此次仅沿用现有配置测试，没有修改正式配置。
- 新增benchmarks/audit_quality/run_agent_check.py：正常AuditService、真实OpenSearch PIT和SQLite源码绑定，合成输入不含预期答案。
- 验收工具使用显式独立索引名，不修改全局正式索引；结束仅清理本次唯一测试索引，保留本地数据库与报告。
- 合成索引使用全文文件块，不调用Embedding，也不声称覆盖生产索引流水线。
- 本轮预算总上限10次，连通性已用1次。Agent检查最多9次，尚未运行；不能自动扩大额度。
- 新增5项离线保护测试，完整回归94项通过。真实模型自主工具链验收尚未完成。
- 当前阻碍：Docker Linux引擎不可用；尝试启动Desktop后仍无可用引擎，需要用户手动启动确认。

## Docker恢复后的首次Agent检查

- 用户重启后Docker与OpenSearch恢复健康。case-a使用真实PIT及绑定源码快照创建成功。
- Agent首个请求耗时3.38秒，随后基线阶段失败；0条已读证据、0条独立复核。不能作为质量通过或反例通过。
- 原始通用错误未保存具体原因，无法追溯认定是格式、工具或其他异常。未自动重试。
- 已补基线ModelOutputError专门失败分类及2项离线测试；这不证明历史请求必然属于该错误。
- 累计真实调用2次（纯文本连通性1次、Agent1次），剩余8次。本轮已停止模型调用。
- 结果保存在data/audit-checks/0b5cfd2952994c49a7959844ec991e10；唯一测试索引已清理，正式索引未修改。
- 下一次真实检查需明确同意再次调用，仍不得超过剩余额度；未部署改动。

## 继续执行结果

- 用户授权继续后，第二次case-a用1次调用，6.27秒结束为model_output_invalid，无已读证据。
- 补固定输出错误码：供应端格式、缺正文、JSON正文、非对象、截断、超大响应、动作协议；不保存原始响应。
- 第三次case-a用5次调用；依次执行list_chunks、read_chunk、audit_progress、read_file，保存2条证据。
- 第4次请求54.99秒，第5次60.08秒后在建立连接或等待响应头阶段超时。不能区分供应商排队和生成慢；未进入独立复核。
- 结果目录：data/audit-checks/2c2ff49725da4ea5a878d55953e30f95；测试索引已清理。没有真实仓库外发。
- 总调用数8/10，剩余2次。已请求新增最多60次合成验收及单请求120秒的授权；尚未执行，也未修改60秒默认值。
- 第2阶段只读工具统一已实现：models/audit_tools.py统一名称、参数和说明，三阶段共用；未接入能力继续拒绝。
- 全部113项测试通过，1个既有测试依赖弃用警告。工具统一是离线验证；最近真实进程使用修改前提示词。
- 计划仍未完成：完整三类真实模型验收、混合搜索版本一致接入、Neo4j/Joern实际服务、长任务工程化及部署验收均不得标记完成。
- 未部署当前源码，现有容器与磁盘代码仍可能不同版本。
