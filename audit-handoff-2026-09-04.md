# Secval 自建审计后端交接

日期：2026-09-04。用户最新要求：停止继续扩展功能，简单调用模型测试（总上限10次），将剩余工作交接。本文不是完成验收声明。

## 1. 项目目标与边界

目标是独立 Web Agent 应用：自建 Python 运行时负责审计编排，搜索/源码快照负责证据，未来接入项目关系与程序路径能力。参考用户提供的 `openai-codex-security-0.1.25.tgz` 的审计方法，不是移植其私有实现，不依赖插件运行。

采用 API → services → models/interfaces → infrastructure 分层。当前只读静态分析，不运行被审计代码、不动态复现、不自动提交修复或发布发现。仍是本机单用户原型，不能直接公开到互联网。

## 2. 已实现流程

1. 创建任务：仓库、快照、可选文件/目录范围、用户安全背景与已有威胁模型；源码外发需显式授权，配置文件需逐路径额外授权。
2. 范围预检：固定 OpenSearch PIT；源码文件只能使用仓库/版本/索引批次精确绑定的 SQLite 快照。旧索引无绑定时不猜磁盘目录。
3. 独立上下文基线：先读取源码、提出有证据的问题，再进入主调查。
4. 主调查：记录安全边界、结构化威胁模型、调查问题、同模型核查及反证、候选详情与文件审阅声明。
5. 独立上下文验证：候选详情与证据包隔离复核，可补读源码。仍是同一模型、静态分析，不是另一模型或动态验证。
6. 报告：仅提升详情指纹与支持性独立复核匹配的候选；保留反驳、未决问题、缺失证据及覆盖缺口。发现仍标记需复核。
7. 续跑：基线/主调查检查点落盘；停止任务可显式授权建立子任务，保留父任务。快照、批次、范围、配置授权和清单须一致。验证阶段中断会回到先前主调查边界，不是原地恢复验证。

## 3. 取证与预算能力

- `list_chunks`、`search_text`（索引短语）、`find_symbol`（完整符号签名）和 `read_chunk`。
- `list_files`、`read_file`：固定文件快照，支持字符续读或可选起止行，不能混用；证据保留来源、位置和内容摘要。
- `search_source`：授权快照内区分大小写的字面搜索，每文件首个命中、每页20文件。只是线索，必须读取才能引用；不是 LSP/调用图。
- `audit_progress`：查询后端未审文件、待调查问题及剩余调用数。未知清单不等于零文件。
- 阅读字符覆盖、文件审阅声明和项目安全覆盖分开；完整读过文件不代表已审计。报告始终不宣称全项目审计完成。
- 默认12次调用/300秒，可显式设置1–300次、30–3600秒；各阶段共享。时长在请求边界检查，不能撤回已发请求，不是费用硬上限。
- 超过80000字符时，可确定性省略旧工具消息中的源码正文；原始证据及审计判断不删，最近四条消息不压缩。无法缩减的上下文仍会停止。

## 4. 关键代码入口

| 职责 | 文件 |
| --- | --- |
| API与测试页面 | `src/secval/web_api/audit_api.py` |
| 创建/取消/续跑 | `src/secval/services/audit_service.py` |
| 主循环/报告提交/独立验证调度 | `src/secval/services/audit_runner.py` |
| 基线与独立验证 | `src/secval/services/baseline_audit.py`、`independent_review.py` |
| 检查点/上下文/待办 | `src/secval/services/audit_checkpoint.py`、`audit_context.py`、`audit_progress.py` |
| 候选提升/报告导出 | `src/secval/services/finding_report.py`、`audit_report.py`、`report_coverage.py` |
| 文件覆盖 | `src/secval/models/file_review.py`、`src/secval/services/file_review_coverage.py` |
| 取证视图/固定源码 | `src/secval/infrastructure/audit/index_evidence_tools.py`、`source_snapshot_store.py` |
| 模型适配/任务存储 | `src/secval/infrastructure/audit/api_audit_model.py`、`sqlite_audit_store.py` |
| 组装/独立审计配置 | `src/secval/bootstrap/audit_runtime.py`、`src/secval/config/audit_settings.py` |

## 5. 已验证与未验证

- 最新完整本地回归：258项通过（包含合成样例答案隔离测试；不代表模型准确率）。后续模型探针脚本尚未纳入该次回归。
- 真实 OpenSearch + SQLite 验证脚本：`benchmarks/recall_baseline/check_snapshot_evidence.py`。验证固定视图、块/文件摘要一致、精确行范围、授权配置、字面搜索及未绑定批次拒绝读取。独立测试索引已清理，未改现有仓库。
- 目录扫描失败会回滚快照，不再静默跳过无权限目录。仍不具备抵抗本地进程并发替换文件的完整防护。
- 真实模型简单测试：首个请求60.09秒后网络失败/超时，未返回可评估结论；随后停止。不能替代模型自主调用工具的完整端到端评测。

## 6. 本次真实模型简单测试

独立审计 API 地址和密钥目前未配置（只检查是否存在，没有显示内容）。用户明确允许测试时复用现有重排序 API 的地址、模型和凭据，不修改正式配置；只发送合成代码，总上限10次，故障不自动重试。

样例在 `benchmarks/audit_quality/cases.py`，探针在 `probe.py`。三类情况：缺失所有权校验、存在有效所有权校验、策略依赖实现缺失。答案字段不进入模型输入。每个请求独立上下文；检查结果标签、引用位置及简洁依据。这是直接提供代码的判断探针，不走基线/工具/调查记录/独立复核完整链路。

**测试状态：已停止，发起1次请求，上限10次，未自动重试。**

| 请求 | 样例 | 预期 | 实际结果 | 耗时 |
| --- | --- | --- | --- | --- |
| 1 | case-a（缺失所有权校验） | 静态支持该越权假设 | `审计API网络失败或超时`，没有模型结论 | 60.09秒 |

剩余9次额度未使用；case-b、case-c未调用。当前适配器单请求超时60秒，错误已脱敏；尚不能区分连接、TLS、服务端排队或生成迟缓，不能断言模型能力不足。未取得准确率或有效误报/漏报指标，不能记为“0%准确率”。请求超时不意味着供应商未计费。

接手先诊断网络与供应商响应时间，再决定是否另行批准重试或调整请求超时。本次授权没有被用于真实仓库外发，也没有将重排序凭据写入独立审计配置。

## 7. 当前部署与配置

- 本地 API：`http://127.0.0.1:8000`，测试页面 `/audit`；搜索测试页面原端口8080。
- 最近已部署版本是在236项测试通过时构建，健康检查通过。后续上下文压缩、目录扫描回滚、扩展预算、待办工具、字面搜索等代码尚未重新部署。不要把磁盘源码与运行容器视为同版。
- Docker 服务：API、OpenSearch、Qdrant；任务/源码数据库使用 audit-data 卷。已有仓库向量未重建。
- 独立审计配置：`SECVAL_AUDIT_API_URL`、`SECVAL_AUDIT_API_KEY`、`SECVAL_AUDIT_MODEL`。本次复用仅为探针，不会自动填入正式审计配置。
- 现有 Jeesite 索引不应假定已绑定源码快照；完整文件工具必须实际预检。需要选择受控索引流程重新建立绑定，不能拿当前目录冒充旧版本。

## 8. 剩余任务（按优先顺序）

### P0：先取得完整流程证据

1. 明确正式审计模型配置并取得真实仓库外发授权；不要把本次合成样例授权扩大为真实代码外发许可。
2. 建立合成项目的真实索引批次/源码绑定，使用正常 AuditService 走完整模型自主工具调用流程。分别核对正例、反例、缺失依赖例；不能把空报告算负例成功。
3. 记录调用量、时延、格式错误、预算耗尽、漏报/误报、关键证据与独立复核是否完整。再做小型真实项目验收。
4. 测试通过后检查无运行任务再更新 API 容器，复测页面、OpenAPI、创建/取消/续跑/导出。不要无授权重建用户现有向量。

### P1：补齐核心审计方法

5. 独立架构/资源分析：入口、资产、信任边界、实际生效配置及消费方关联；区分源码事实、用户前提和假设。目前结构化威胁模型不是完整架构分析器。
6. 有效策略与框架入口：继承/过滤器/拦截器/代理/路由、外部策略实现、依赖缺口；不能仅凭局部缺少注解判断漏洞。
7. 确定性符号/调用者/数据流能力：评估 LSP、AST 关系、Neo4j/Joern 分工。当前只具备搜索与手工补证，不能宣称跨函数路径分析完整。
8. 报告结束门槛：依据授权范围、基线问题、边界、文件与候选验证逐项收口；不能简单把 `complete` 改为 true。明确“提交部分报告”和“完成所有计划检查”的区别。

### P2：长任务可靠性与工程化

9. 大项目按范围/调查问题拆分调度、分阶段预算、验证预算预留；当前扩大上限不等于完整的大项目调度。
10. 验证阶段原地恢复、已完成候选复核复用、上下文记录量控制。当前子任务可能重复工作；复核状态仍需准确绑定详情版本。
11. 严格请求期限、token/费用核算及总任务链预算；当前只有请求次数与请求边界时长限制。
12. 源码采集并发硬化、跨索引/快照事务一致性、失败孤立快照清理和保留策略；字面搜索重复扫描性能优化。
13. 多用户隔离、认证、访问控制、敏感内容策略与报告保护，完成前保持仅本机使用。

## 9. 接手注意事项

- 工作区包含大量用户既有修改、删除及未跟踪文件；没有提交，不可 reset/checkout 覆盖。先检查差异来源。
- `.gitignore` 忽略新增测试文件及大部分 docs；本交接文档和实施状态文档已单独放行。若需要团队复现，应明确决定纳入哪些测试/参考文档，不能以本机文件存在推断已进版本库。
- 不查看/打印 `.env`、API密钥或完整容器环境；不要将含源码报告公开上传。
- 未进行真正动态漏洞复现，未证明达到 Codex Security 等效能力，不能以测试数或单次模型表现代替完整验收。
- 原总体目标仍未完成。按用户最新要求，本次以简单测试与交接收尾，不继续自行扩展实现。
