# 多Agent协作审计：实现与使用说明

参考固定版本：Codex Security `npm-v0.1.25` 的 `core-scan.md` 与 `threat-model.md`。
这是Secval自建实现，不调用Codex的扫描服务，也不宣称与其完整能力相同。

## 当前执行顺序

1. Web创建审计，固定仓库、源码快照、索引批次与授权路径。
2. 后台建立一个主Agent和有数量上限的子Agent池。默认同时3个，包含主Agent。
3. 独立基线与架构任务先启动；主Agent同时检查其他入口，不先等待基线完成。
4. 主Agent发现具体源码起点后，用`start_investigator`分派专项问题，立刻返回继续工作。
5. 每个子Agent自行循环调用只读工具，有独立模型实例、对话、证据和检查点。不能再创建孙任务。
6. 子任务结果先落盘。主Agent下一轮请求前接收一项结构化结果，不复制子Agent的完整聊天历史。
   子Agent查清一组候选、反证或未知项后可先调用`submit_worker_progress`。阶段成果经证据校验后立即持久化，
   不依赖最后一次完整JSON成功；主Agent下一轮只接收一条精简阶段回注。
7. 问题带稳定编号，沿用`baseline_question_ids`关联后续调查。这个兼容字段现在也容纳专项和架构待核查问题。
8. 主Agent检查源码、记录边界与调查、核对候选详情。支持的候选仍需要最终独立上下文复核。
   多个具备完整候选详情的复核使用剩余Agent槽并行执行；每个复核有独立模型实例和局部证据缓冲，
   主线程按调查顺序合并，仍共享同一总调用预算和固定取证范围。
9. 未完成、失败、未接续的问题保留在报告缺口里；提交报告不等于完整审计。

架构观察通过结构化结果中的摘要、问题与证据引用返回；主Agent核对后写入现有结构化威胁模型。
目前没有照搬上游完整的架构资源表格式。混合搜索、Neo4j声明关系、Joern调用位置和静态数据流已经作为只读线索工具接入；都绑定固定批次，不能代替源码证据。

## 为什么这样拆分文件

- `models/agent_work.py`：检查分派任务和子任务结果，拒绝未读取的证据引用。
- `services/agent_worker.py`：一个子Agent如何反复读取代码并返回结果。
- `services/agent_team.py`：调度、共享预算、结果交付、取消和结束清理。
- `services/audit_runner.py`：主调查仍使用现有记录、证据和报告校验；增加协作工具入口。
- `services/audit_service.py`：在Web任务创建时组装协作运行时，续跑时恢复子任务记录。

## 上下文交接

下发：目标、授权范围、用户前提、任务问题、起始源码位置和只读工具。
不下发：主Agent完整历史、其他子Agent对话、未经选择的源码。

回传：摘要、问题状态、证据编号、反证、未知项、完整审阅声明。
源码证据在统一范围校验后保存，主上下文接收定位信息，必要时重新读取实际源码。
完整子结果与证据保留在数据库及报告，不能因精简主上下文丢失反证。
每个结果最多18000字符；过长时要求精简，不静默截断结论。
阶段成果有稳定的`agent-N:progress-M`编号，已交付编号与主检查点一起保存，因此不会重复注入。
子任务最后失败时，已保存阶段成果仍留在报告中，但该子任务继续算作覆盖缺口。

已交付编号与主检查点在同一次存储更新中保存。恢复时已完成子任务不重跑；失败或中断者保留自己的已保存对话重试。
旧失败交付记录在恢复时解除标记，允许该子任务完成后重新交付；父任务历史不修改。

## 预算和资源

- `parallel_agents`：默认Web值3，允许1～4；1用于历史串行兼容，正常页面选择2～4。
- Python旧调用和旧任务保留默认1，不在恢复时悄悄变成并发任务。
- `max_steps`：全队总模型调用额度，包含基线、架构、专项、主调查和复核。
- 子任务独立调用上限为总额度的三分之一，上限12次；全队保留部分调用给主调查及复核。
- 每个请求发送前在锁内占用额度、分配唯一调用编号，完成后更新该条记录。
- 不共享模型实例，避免不同请求的统计和上下文混淆。
- 子Agent共享同一个固定取证视图。短工具读取加锁，模型请求并行；不是并行修改索引。
- 最多12个子任务，不递归扩增Agent。问题相同且任务资料相同的分派复用已有任务。
- 等待子任务不额外调用模型，每秒检查取消和任务时间预算。
- 正在发送的网络请求不能保证立即撤回或不计费。任务取消后不接收迟到结果，新任务等待旧请求退出。
- `execution_active`区别任务状态与后台是否真正退出，部署前也要检查该字段。

网络请求仍可能失败。多Agent不保证供应商生成速度，也不自动增加超时、token或总调用预算。

## 测试与小demo

`tests/demo_projects/team_orders/`只有两个Java文件：一个归属控制缺失样例、一个含归属比较的反例。
不执行应用、不编译、不使用真实仓库。README答案说明不进入上传ZIP。

离线测试：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/audit/test_agent_team.py -q
```

正式Web合成验收（会调用已配置的远程模型）：

```powershell
.\.venv\Scripts\python.exe -m benchmarks.audit_quality.run_team_demo --allow-model-calls
```

每次运行创建独立UUID仓库，不覆盖或删除用户仓库；测试数据保留供页面查看。
用`benchmarks.audit_quality.run_web_check --collect <返回的目录>`收集报告。
脚本模型证明编排正确，不代表真实模型判断正确。真实报告要分别核对控制缺失、有效反证及未知前提。
测试中的两个独立候选使用并发屏障：只有两项复核同时进入模型请求才能通过，用来防止以后退化回串行。

## DSH continuable 对照结论

参考DeepSeek Harness官方quickstart、`docs/subsystems/subagent.md`与宿主
`packages/subagent/subagent/src/continuation.ts`。`backgroundMode: continuable`不是父Agent轮询：

1. 创建时只等初始消息被子Agent收件箱接受，不等待执行完成。
2. 子Agent拥有可持久化Session和可恢复的Activation，所有后续消息走自己的FIFO收件箱。
3. 子Agent可在结束前用`send_message`向父Agent发送阶段结果；父Agent不会自动收到其完整对话和工具输出。
4. 宿主监听子Agent空闲及子孙任务释放。结束时先刷新子Session，再生成带来源的结束通知。
5. 父Agent空闲时用`followup`启动一轮；忙碌时用`steer`在下一步骤接收，因此不需要反复查询。

Secval不是会话型Agent宿主，不能直接复制它的Activation对象；这里对应采用“数据库阶段成果、changed事件、
主循环下一模型请求前交付”。两者共同原则是：生产者主动通知、结果先持久化、父任务不轮询模型。

当前模型适配仍使用普通JSON正文协议。只有确认具体供应商支持的`tools/tool_calls`请求和返回格式后，
才会增加原生工具调用；不能仅凭“OpenAI-compatible”名称改变正式链路。

## 已知边界

关系与路径工具：`hybrid_search`使用BM25、向量和RRF；`find_code_relations`查询Neo4j声明关系；`find_code_calls`与`find_data_paths`查询Joern。模型不能执行Cypher或CPGQL。索引替换顺序为固定源码快照、OpenSearch、Qdrant、Neo4j、Joern、源码绑定，随后才清理旧批次。

这是单进程、单个根审计、多子Agent实现，不是分布式队列；容器重启后标记中断，通过检查点显式续跑。
独立复核使用同一配置模型的新上下文，不是不同供应商，也不是动态攻击验证。
阅读覆盖仍基于实际证据和模型的审阅声明，不是数学意义上的安全性证明。
